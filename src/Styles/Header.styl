.app-header {
    width: 100%;
    height: 10vh;
    display: flex;
    padding: 0 35px;
    flex-flow: row nowrap;
    justify-content: space-between;
    align-items: center;
    background-color: $tertiary;

    input[type="search"] {
        width: 320px;
        height: 60px;
        border-radius: 5px;
        border: 1px solid black;
        padding: 0 rem(20);
        font-size: rem(20);
    }
}
